# Avatar Persistence

Avatar configuration is now part of the persisted Family Member record.

- Avatar editor updates save through the Family Member update API.
- Avatar rendering uses the loaded persisted member data.
- Existing fallback avatar values remain available for offline-tolerant loading failure behavior.
