# Family Goal Creation

Implemented the minimum viable creation path for one active family goal. The Motivation API now accepts title, target count, progress wording, and an optional family celebration label, creates a new active household goal with zero starting progress, and deactivates any prior active family goal to preserve the one-active-goal rule.

The Motivation page empty state opens a simple family-friendly form directly from `Create your first family goal`.
