# FamilyBoard decorations

This directory is reserved for future SVG-only decorative motifs such as soft blobs, confetti, rays, and reusable card accents. This foundation slice intentionally does not populate decorations.
