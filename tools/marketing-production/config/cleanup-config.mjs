export const cleanupConfig = Object.freeze({
  policy: 'remove-temporary-artifacts',
  removeIntermediateFiles: true,
  removeTemporaryRecordings: true,
  removeTemporaryWavs: true,
  removeTemporaryBrowserProfiles: true,
  removeTemporaryToolchain: false,
  retainMetadata: true,
  retainTimingJson: true,
});
